<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- List View -->
    <record id="view_pml_product_list" model="ir.ui.view">
        <field name="name">pml.product.list</field>
        <field name="model">pml.product</field>
        <field name="arch" type="xml">
            <list string="Products">
                <field name="name"/>
                <field name="sales_price"/>
                <field name="cost_price"/>
                <field name="version"/>
                <field name="status" widget="badge"
                       decoration-success="status == 'active'"
                       decoration-muted="status == 'archived'"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_pml_product_form" model="ir.ui.view">
        <field name="name">pml.product.form</field>
        <field name="model">pml.product</field>
        <field name="arch" type="xml">
            <form string="Product">
                <header>
                    <button name="action_archive" string="Archive"
                            type="object" class="btn-secondary"
                            invisible="status == 'archived'"/>
                    <button name="action_unarchive" string="Unarchive"
                            type="object" class="btn-secondary"
                            invisible="status == 'active'"/>
                    <field name="status" widget="statusbar"
                           statusbar_visible="active,archived"/>
                </header>
                <sheet readonly="status == 'archived'">
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_ecos" type="object"
                                class="oe_stat_button" icon="fa-cogs"
                                invisible="eco_count == 0">
                            <field name="eco_count" widget="statinfo" string="ECOs"/>
                        </button>
                    </div>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="Product Name..."/>
                        </h1>
                    </div>
                    <group>
                        <group string="Pricing">
                            <field name="sales_price"/>
                            <field name="cost_price"/>
                            <field name="currency_id" invisible="1"/>
                        </group>
                        <group string="Info">
                            <field name="version"/>
                            <field name="status"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Attachments" name="attachments">
                            <field name="attachment_ids" widget="many2many_binary"/>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <!-- Action -->
    <record id="action_pml_product" model="ir.actions.act_window">
        <field name="name">Products</field>
        <field name="res_model">pml.product</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first Product!
            </p>
        </field>
    </record>

</odoo>
